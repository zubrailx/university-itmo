#include <gtest/gtest.h>

extern "C" {
}

TEST(types, sizes) {
  // printf("sizeof(fileoff_t): %zu\n", sizeof(fileoff_t));
  // printf("sizeof(pageoff_t): %zu\n", sizeof(pageoff_t));
  // printf("sizeof(bodyoff_t): %zu\n", sizeof(bodyoff_t));
}
