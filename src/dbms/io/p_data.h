#pragma once

#include "p_base.h"
#include "page/p_data.h"

// PAGE_DEFAULT_SELECT(struct data_page, da)
// PAGE_DEFAULT_CREATE(struct data_page, da)
// PAGE_DEFAULT_ALTER(struct data_page, da)
// PAGE_DEFAULT_DROP(struct data_page, da)

// PAGE_DEFAULT_CONSTRUCT_SELECT(struct data_page, da)
// PAGE_DEFAULT_ALTER_DESTRUCT(struct data_page, da)
